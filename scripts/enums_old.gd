class_name Enums_OLD


enum item {
    invalid = -1,
    none = 0,
    star_key,
    diamond_key,
    square_key,
}


enum tag {
    none = 0,
    flamable,
    wet,
    fire,
    on_fire,
    locked,
    unlocked,
    moveable,
    pickup,
    key,
    consumable,
    cannot_drop,
}
